<%- include('partials/header') %>
<div class="container">
  <div class="form-page">
    <div class="form-card">
      <h1>Esqueci minha senha</h1>
      <% if (success) { %>
        <p class="form-hint">Se existir uma conta com esse e-mail, em breve você receberá instruções para redefinir sua senha. Verifique sua caixa de entrada e o spam.</p>
        <p><a href="/login" class="btn btn-primary">Voltar ao login</a></p>
      <% } else { %>
        <% if (typeof error === 'string' && error) { %><p class="error"><%= error %></p><% } %>
        <p class="form-hint">Informe seu e-mail cadastrado. Em uma próxima versão enviaremos um link para redefinir sua senha.</p>
        <form action="/esqueci-senha" method="post" class="form">
          <label for="email">E-mail</label>
          <input id="email" type="email" name="email" required placeholder="seu@email.com" autocomplete="email">
          <button type="submit" class="btn btn-primary">Enviar instruções</button>
        </form>
        <p class="form-footer"><a href="/login">Voltar ao login</a></p>
      <% } %>
    </div>
  </div>
</div>
<%- include('partials/footer') %>
